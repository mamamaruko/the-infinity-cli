---
description: v1.5.86 | Research-first product design skill without mandatory MCP. Use when user says "design this", "create UI direction", "analyze UX", "make a design brief", or "improve interface".
---

# /design

Execute the `design` skill with the provided arguments.

## Instructions

**If you have a Skill tool available**: Use it directly with `skill: "design"` instead of reading the file manually.

**Otherwise**:
1. Read the skill file at this exact path: `~/.claude/skills/design/SKILL.md`
2. Follow all instructions in the skill file
3. Pass these arguments to the skill: `$ARGUMENTS`

**IMPORTANT**: Only use the path above. Do NOT search for or use any other file with "design" in the name.

---
*the-infinity-cli v1.5.86*
