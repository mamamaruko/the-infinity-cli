# ψ/ - AI Brain for the-infinity-skills-cli

> Born: 2026-01-20
> Parent: mamamaruko/the-infinity-skills-cli

## Structure

```
ψ/
├── active/         <- Research in progress (gitignored)
│   └── context/
├── inbox/          <- Communication and handoffs
└── memory/         <- Knowledge base
    ├── retrospectives/  <- Session summaries (rrr)
    ├── learnings/       <- Patterns discovered
    └── logs/            <- Activity logs
```

## Usage

```bash
# End of session
rrr   # Creates retrospective in ψ/memory/retrospectives/

# Quick notes
# Add learnings to ψ/memory/learnings/YYYY-MM-DD_topic.md
```

## Git Status

| Folder | Tracked |
|--------|---------|
| ψ/active/* | No |
| ψ/inbox/* | Yes |
| ψ/memory/* | Yes |

## Historical Policy

- `ψ/memory/` and `ψ/inbox/` are historical records.
- Older files may include legacy names (for example `oracle-skills-cli`) as part of accurate timeline context.
- Normalize file naming and metadata for new entries, but avoid rewriting historical narrative unless the content is factually wrong.
